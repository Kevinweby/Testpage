angular.module('myapp').controller('homeController',['$scope', '$http', '$window', '$rootScope', 'appFactory', '$filter',
    function ($scope, $http, $window, $rootScope, appFactory,$filter, DTOptionsBuilder) {
    	var us = localStorage.getItem("us");
         
    }
]);